import { atom } from 'jotai';

// Minimal placeholder state used by the floating timer window.
export const timerPanelVisibleAtom = atom(true);
export const timerPanelMessageAtom = atom('Timer Placeholder');
